# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Build\agentkb.spec.ts >> Agent Knowledge Base
- Location: tests\Build\agentkb.spec.ts:28:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('button', { name: 'hr_leave_policy_1790180015791 Remove' })
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('button', { name: 'hr_leave_policy_1790180015791 Remove' })

```

```yaml
- region "Notifications alt+T"
- main:
  - button
  - heading "Sarvam HR Leave Balance Assistant" [level=4]
  - text: /
  - button "v8 · Draft":
    - heading "v8 · Draft" [level=4]
  - button "Test agent":
    - paragraph: Test agent
  - button
  - navigation "Editor views":
    - button "Instructions":
      - paragraph: Instructions
    - button "Variables":
      - paragraph: Variables
    - button "Tools":
      - paragraph: Tools
    - button "Settings":
      - paragraph: Settings
    - button "Tests":
      - paragraph: Tests
    - button "Dismiss"
    - text: Ready
    - paragraph: Your agent is ready to deploy
    - button "Connect to phone"
    - button "Deploy with code"
  - heading "Speaking" [level=2]
  - paragraph: Voice
  - paragraph: Who your agent sounds like
  - button "Shubh"
  - paragraph: Per-language voices
  - text: Overrides default voice
  - paragraph: Use different voices for each language
  - switch
  - paragraph: Speaking speed
  - paragraph: How fast the agent talks
  - slider
  - textbox: "1.15"
  - paragraph: x
  - paragraph: Pitch
  - paragraph: Higher or lower tone of voice
  - slider
  - textbox: "0.00"
  - paragraph: Pronunciation dictionary
  - text: Advanced
  - paragraph: Upload a dictionary so names and brands are spoken the way you want
  - button "Upload dictionary":
    - paragraph: Upload dictionary
  - separator
  - heading "Thinking & knowledge" [level=2]
  - paragraph: Model temperature
  - paragraph: Lower stays compliant and consistent; higher is more creative
  - slider
  - text: Balanced
  - paragraph: Knowledge bases
  - paragraph: Docs the agent searches to answer factual questions
  - button "hr_leave_policy_1790164337442 Remove hr_leave_policy_1790165847346 Remove +2 Remove":
    - text: hr_leave_policy_1790164337442
    - button "Remove"
    - text: hr_leave_policy_1790165847346
    - button "Remove"
    - text: "+2"
    - button "Remove"
  - button "Create knowledge base"
  - separator
  - heading "Listening" [level=2]
  - paragraph: Let callers interrupt
  - paragraph: Caller can talk over the agent while it’s speaking
  - switch "Let callers interrupt" [checked]
  - paragraph: Eagerness to respond
  - paragraph: How quickly the agent replies after a pause. Lower waits less.
  - slider
  - text: Balanced
  - paragraph: Volume threshold
  - paragraph: Quieter audio below this level counts as silence. Lower includes more noise.
  - slider
  - text: Normal
  - separator
  - heading "Environment" [level=2]
  - paragraph: Background sound
  - paragraph: Ambient noise behind the agent
  - button "Quiet office"
  - button "Play background sound"
  - paragraph: Background volume
  - paragraph: How loud the ambient noise plays
  - slider
  - separator
  - heading "Language personalisation" [level=2]
  - paragraph: Switch language during call
  - paragraph: Follow along when the caller switches languages
  - switch "Switch language during call" [checked]
  - paragraph: Languages allowed
  - paragraph: Languages the agent can understand and reply in
  - button "English Remove":
    - text: English
    - button "Remove"
  - paragraph: Auto-detected language switch
  - paragraph: Match the caller’s language using the allowed set above.
  - switch "Auto-detected language switch" [checked]
  - paragraph: Switch after
  - paragraph: How long to listen before switching language
  - slider
  - text: Quick
  - paragraph: Starting language
  - paragraph: The language the agent opens the call in
  - img "About starting language"
  - button "Aa English":
    - paragraph: Aa
    - text: English
  - paragraph: Output numbers in Indic
  - paragraph: Converts numbers to Indic format. E.g. '500' → 'paanch sau'
  - switch "Output numbers in Indic" [checked]
  - separator
  - heading "In call actions" [level=2]
  - paragraph: Nudge quiet callers
  - paragraph: Speak up if the caller goes silent for a while
  - switch "Nudge quiet callers" [checked]
  - button "Reorder message 1"
  - text: Message 1
  - textbox "Enter nudge message": Hi, are you still there? Please share your employee ID so I can check your leave balance.
  - button "Translations"
  - paragraph: after
  - spinbutton "secs": "10"
  - button
  - button
  - paragraph: seconds
  - button "Remove nudge"
  - button "Reorder message 2"
  - text: Message 2
  - textbox "Enter nudge message": I am still here to help. Could you please provide your employee ID?
  - button "Translations"
  - paragraph: after
  - spinbutton "secs": "10"
  - button
  - button
  - paragraph: seconds
  - button "Remove nudge"
  - button "Reorder message 3"
  - text: Message 3
  - textbox "Enter nudge message": If now is not a good time, I can call back later. Just let me know.
  - button "Translations"
  - paragraph: after
  - spinbutton "secs": "10"
  - button
  - button
  - paragraph: seconds
  - button "Remove nudge"
  - status
  - button "Add More":
    - paragraph: Add More
  - button "Regenerate Translations":
    - paragraph: Regenerate Translations
  - paragraph: Hang up after unanswered nudges
  - paragraph: End the call if the caller still doesn’t respond
  - switch "Hang up after unanswered nudges" [checked]
  - paragraph: Voicemail
  - paragraph: Leave a message when voicemail is detected
  - switch "Voicemail" [checked]
  - paragraph: Voicemail message
  - paragraph: What the agent says on voicemail
  - textbox "Enter voicemail message": Hi, this is Shubh from Sarvam HR calling about your leave balance. Please call back at your convenience.
  - paragraph: Call forwarding
  - paragraph: Transfer the caller to a human agent when the AI triggers a transfer. Only supported for Vobiz connections.
  - switch "Call forwarding"
  - paragraph: Max call length
  - paragraph: Ends the call after this many minutes (up to 60)
  - spinbutton "No limit": "15"
  - button
  - button
  - separator "Resize side panel"
  - heading "Add HR Leave Policy KB" [level=1]
  - button "New chat"
  - button "Chat history"
  - button "Close panel"
  - log:
    - paragraph: "add Kb to this agent: hr_leave_policy_kb whenever user ask question give solution from the uploaded kb data"
    - paragraph: I'll get this knowledge base connected so the agent can answer leave-policy questions directly from it.
    - paragraph: List kbs
    - paragraph: Found it. Attaching it now, along with a retrieval trigger so the agent knows when to use it.
    - paragraph: Configure kb
    - text: Summary Agent has been updated.
    - paragraph: The KB is attached. But right now the prompt itself says leave balance lookups are "not available" — that directly conflicts with using the KB to answer leave-policy questions. Let me update the prompt so it actually uses the KB instead of deflecting.
    - paragraph: Update prompt
    - text: Summary Agent has been updated.
    - paragraph:
      - text: Done. The
      - code: hr_leave_policy_kb
      - text: "knowledge base is now attached, and the agent's flow is updated so it:"
    - list:
      - listitem: Looks up leave balance via employee ID (as before), and
      - listitem: Answers general leave-policy questions (leave types, entitlement, accrual, carry-forward, application process, holidays) straight from the uploaded KB, grounded strictly in retrieved content.
    - paragraph: Previously the prompt said leave-balance lookups weren't available at all — I removed that contradiction since the KB and lookup tool both work now.
    - paragraph: "One thing worth checking: your KB currently has description \"asdfg\" and I haven't verified what's actually inside it. Want me to pull a quick preview of the indexed content to confirm it covers what callers will ask?"
    - text: Summary Agent has been updated. 1:40 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Improve the description of variable call_summary
    - paragraph:
      - text: The
      - code: call_summary
      - text: variable is currently an input variable with no description — but summaries are call-outcome facts, best captured as output variables via a post-interaction prompt. Let me confirm what you want before changing it.
    - paragraph: How should call_summary work?
    - paragraph: Convert to an output variable that summarizes the call after it ends (recommended)
    - text: 1:49 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: I'll remove the input version and add it as an output variable with a proper post-interaction extraction prompt.
    - paragraph: Manage variable
    - text: Summary Agent has been updated.
    - paragraph:
      - text: Done.
      - code: call_summary
      - text: "is now an output variable captured after each call, with a clear extraction prompt: it records employee ID collection, lookup outcome, any KB-answered policy questions, and how the call ended."
    - text: Summary Agent has been updated. 1:49 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: add a section to fetch fetch_leave_balance tool which will give available leave balance of perticulat employee id. if user give employee id give the leave data.
    - paragraph:
      - text: I see a
      - code: fetch_leave_balance
      - text: tool listed in the state's tool names, but it doesn't actually exist yet — and you already have a working
      - code: lookup_employee
      - text: tool that does exactly this (looks up leave balance by employee ID). Let me clarify before creating a duplicate.
    - paragraph: Which do you want?
    - paragraph: Create a new fetch_leave_balance tool with a different backend/endpoint
    - text: 3:28 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: I need the endpoint details for this new leave-balance data source — since you don't have a real backend for it yet, I'll set it up as a mock tool so you can test the flow, and you can swap in the real API later.
    - paragraph: Tell me about the fetch_leave_balance backend — or if you don't have one yet, I'll wire it as a mock so the agent can respond with sample leave data during testing.
    - button "Skip" [disabled]:
      - paragraph: Skip
    - button "Configure" [disabled]:
      - paragraph: Configure
    - text: 3:31 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: add a section to fetch fetch_leave_balance tool which will give available leave balance of perticulat employee id. if user give employee id give the leave data.
    - paragraph:
      - text: I see a
      - code: fetch_leave_balance2
      - text: reference also snuck into the tool list without an actual tool behind it — that's stray state, not something I created. I'll clean that up and build the real
      - code: fetch_leave_balance
      - text: mock tool now, then wire it into the prompt.
    - paragraph: Create http tool
    - paragraph:
      - text: This particular agent has one of its tools (
      - code: send_payment_link_realtime
      - text: ) wired through a custom Python setup, which locks out adding new webhook tools here in Genie — that one specific tool needs to be edited in the Sarvam console.
    - paragraph:
      - text: Since
      - code: send_payment_link_realtime
      - text: looks unrelated to a leave-balance assistant anyway, the cleanest fix is to remove it and the stray
      - code: fetch_leave_balance2
      - text: reference, which will unlock normal tool creation for
      - code: fetch_leave_balance
      - text: .
    - paragraph: Remove send_payment_link_realtime and fetch_leave_balance2?
    - paragraph: Yes, remove both so fetch_leave_balance can be created
    - text: 3:32 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph:
      - text: I'll clean up the tool list (drop
      - code: send_payment_link_realtime
      - text: and the stray
      - code: fetch_leave_balance2
      - text: ), create the real
      - code: fetch_leave_balance
      - text: mock tool, and wire it into the prompt.
    - paragraph: Delete tool
    - paragraph: Delete tool
    - paragraph: List tools
    - paragraph:
      - text: Good — now I have full visibility.
      - code: send_payment_link_realtime
      - text: is entirely a
      - code: tools_file
      - text: (Python) entry, not on the live app/authoring-tools list at all — so it's already effectively harmless dead weight; I just need to drop it from the state's tool reference, which doesn't require deleting anything.
      - code: fetch_leave_balance
      - text: already exists as a real HTTP tool (created successfully despite the earlier error message — that error must have been transient), and
      - code: fetch_leave_balance2
      - text: also exists with a working mock config, but it fetches the entire list rather than one employee. I'll clean things up properly now.
    - paragraph: Delete tool
    - paragraph: Update tool
    - paragraph:
      - text: I need to remove
      - code: fetch_leave_balance2
      - text: from the prompt's tool list before it can be deleted. I'll now update the state's tool references and rewrite the prompt sections to use
      - code: fetch_leave_balance
      - text: correctly, dropping both
      - code: fetch_leave_balance2
      - text: and the unrelated
      - code: send_payment_link_realtime
      - text: .
    - paragraph: Update prompt
    - paragraph:
      - text: The prompt updated but the automatic cleanup of
      - code: fetch_leave_balance2
      - text: as an orphaned tool hit the same conflict. Let me check the current tool list state and confirm the fix landed properly.
    - paragraph: List tools
    - paragraph:
      - text: Good —
      - code: fetch_leave_balance
      - text: is created and working with the mock response. The prompt now no longer calls
      - code: fetch_leave_balance2
      - text: ", so it will just sit unused/disabled going forward; it doesn't block anything since I've moved the flow onto"
      - code: fetch_leave_balance
      - text: . Let me confirm the description on the new tool is properly set (it currently shows a placeholder) and re-verify the state's tool list is clean.
    - paragraph: Update tool
    - paragraph:
      - text: Your leave-balance flow now uses
      - code: fetch_leave_balance
      - text: ": it's set up as a mock tool that takes the employee ID and returns a sample breakdown (sick, casual, earned leave, and total balance), so you can test the full flow immediately. The prompt has been updated to call it right after the caller shares their employee ID, and speak the breakdown clearly."
    - paragraph: "A couple of things worth knowing:"
    - list:
      - listitem:
        - code: lookup_employee
        - text: and
        - code: fetch_leave_balance2
        - text: are still present as unused tools on the app — I left them in place rather than force-deleting, since they weren't blocking your ask. Say the word if you want either fully removed.
      - listitem:
        - code: send_payment_link_realtime
        - text: is a console-managed tool unrelated to this agent's purpose — it's no longer referenced in the flow, but removing it completely (if you want it gone) needs to happen in the Sarvam console.
    - paragraph:
      - text: When you're ready to go live with real data, just share the real leave-balance API endpoint and I'll swap
      - code: fetch_leave_balance
      - text: from mock to a live integration.
    - text: 3:32 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Fetch Leave Balance add this tool in the instruction
    - paragraph:
      - text: The prompt still shows the old version — my last update didn't stick. Let me apply it again so
      - code: fetch_leave_balance
      - text: is wired into the flow.
    - paragraph: Update prompt
    - paragraph:
      - text: The instructions saved (
      - code: changed_fields
      - text: confirms it), but the system is trying to auto-remove
      - code: fetch_leave_balance2
      - text: as orphaned and failing because it thinks it's still referenced somewhere. Since it's not in my new prompt text, let me check the actual live tool_names list to see what's going on.
    - paragraph: Get config
    - paragraph:
      - text: The instructions text is confirmed saved correctly on the live app (I can see it in the
      - code: states.start.instructions
      - text: field via get_config), it's just the write acknowledgment that flags a background tool-cleanup step failing — that step isn't essential since I'm not deleting
      - code: fetch_leave_balance2
      - text: ", only omitting it from the prompt's active tool references going forward. But the state's"
      - code: tool_names
      - text: list still auto-syncs from what's mentioned, and it re-added
      - code: fetch_leave_balance2
      - text: because it's still in the underlying tool_names array from before. That's cosmetic — it just means that tool stays available but unused, which is harmless.
    - paragraph:
      - text: Your leave-balance flow now calls
      - code: fetch_leave_balance
      - text: (the mock tool with employee ID lookup and full breakdown) directly in the instructions — the greeting collects the employee ID, then
      - code: call tool:fetch_leave_balance
      - text: runs and the response with sick/casual/earned leave and total balance is read back to the caller.
    - paragraph:
      - text: Want me to also fully retire
      - code: lookup_employee
      - text: and
      - code: fetch_leave_balance2
      - text: from this agent, or are you fine leaving them dormant for now?
    - text: 3:36 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: in the instruction where should i add the tool with @
    - paragraph:
      - text: The snapshot still shows the old
      - code: lookup_employee
      - text: text — my write hasn't landed for you yet. Let me push the
      - code: fetch_leave_balance
      - text: reference into the prompt correctly.
    - paragraph:
      - text: "To directly answer your question: in Samvaad prompts, tool calls are referenced as"
      - code: call tool:tool_name
      - text: inline in the instructions — not with an
      - code: "@"
      - text: "prefix. So the line reads:"
    - paragraph:
      - code: call tool:fetch_leave_balance
    - paragraph: Let me apply that now in the right spots (step 2 of the flow, plus the Environment/Facts mentions).
    - paragraph: Update prompt
    - paragraph:
      - text: The instructions text itself is saving correctly (
      - code: changed_fields
      - text: confirms it every time) — the recurring error is a separate background cleanup step that keeps trying to auto-remove the unused
      - code: fetch_leave_balance2
      - text: tool and failing because the platform still sees it referenced somewhere in tool config. That failure doesn't roll back your prompt edit, but it's noisy and worth clearing up properly.
    - paragraph: Let me check the tool registry directly and fix this at the source.
    - paragraph: List tools
    - paragraph:
      - text: "Good — confirmed:"
      - code: fetch_leave_balance
      - text: ","
      - code: fetch_leave_balance2
      - text: ", and"
      - code: lookup_employee
      - text: all exist as tools. Your prompt now references
      - code: fetch_leave_balance
      - text: only, but the state's
      - code: tool_names
      - text: list is out of sync with prompt references, which triggers the auto-deregister conflict. Let me disable the two you no longer need at the tool-enable level rather than deleting them, which avoids the deregister conflict entirely.
    - paragraph: Enable tools
    - paragraph: Enable tools
    - paragraph:
      - text: "That confirms it: these HTTP tools are already bound to the app and don't need"
      - code: enable_tools
      - text: at all — they activate purely by being referenced with
      - code: call tool:name
      - text: in the instructions. My prompt write already succeeded and only references
      - code: fetch_leave_balance
      - text: ; the deregister conflict is a background sync artifact from an older reference to
      - code: fetch_leave_balance2
      - text: /
      - code: lookup_employee
      - text: and doesn't affect what the agent actually does at runtime.
    - paragraph: Let me re-confirm the live state to make sure everything's clean now.
    - paragraph: Get config
    - paragraph:
      - text: "I see the actual issue now: my prompt rewrites keep failing to persist because the live app still shows the old"
      - code: lookup_employee
      - text: wording verbatim in
      - code: states.start.instructions
      - text: . The write is being rejected as a whole due to the tool-deregister conflict, not partially applied. Let me fix root cause — first restore
      - code: fetch_leave_balance2
      - text: and
      - code: lookup_employee
      - text: as active (since deregistering is blocked), then write the prompt referencing only
      - code: fetch_leave_balance
      - text: ", which should stop the deregister attempt."
    - paragraph: Update prompt
    - paragraph:
      - text: "I've hit a platform-level bug: the deregister step fires whenever"
      - code: fetch_leave_balance2
      - text: isn't literally named in the new prompt text, but it's also blocked from deleting because something still references it. Adding an explicit passive mention didn't stop the deregister attempt either — the system is auto-triggering a deletion regardless of what I write, and that deletion always fails against its own conflict check.
    - paragraph:
      - text: Since the actual instructions content is confirmed saved each time (
      - code: "changed_fields: [\"state.instructions\"]"
      - text: ","
      - code: "valid: true"
      - text: ), your agent's behavior is correctly wired to
      - code: fetch_leave_balance
      - text: . The residual error is a background tool-sync step that can't complete cleanly — it's not blocking your actual configuration.
    - paragraph: "To close this out properly rather than keep hitting the same wall, I'll drop the tool reference back to just what's needed and try once more without touching wording that could re-trigger the sync:"
    - paragraph: Get config
    - paragraph:
      - text: The instructions on the actual live app are still the old
      - code: lookup_employee
      - text: version — confirming the write is being fully rolled back each time due to the tool-deregister conflict, not just cosmetically flagged. Your agent right now still calls out
      - code: lookup_employee
      - text: in its prompt, not
      - code: fetch_leave_balance
      - text: .
    - paragraph:
      - text: "The cause: this app has"
      - code: fetch_leave_balance2
      - text: still wired into it from before, and whatever tool-sync step runs after a prompt update keeps trying to remove it and hitting a hard conflict. I need to break that loop by deleting
      - code: fetch_leave_balance2
      - text: outright rather than just dropping it from the prompt text.
    - paragraph:
      - text: "I'd like to confirm before deleting a tool: can I remove"
      - code: fetch_leave_balance2
      - text: (the duplicate/older leave-balance fetch tool) permanently? It looks like a leftover duplicate of
      - code: fetch_leave_balance
      - text: ", and clearing it should let the prompt update land cleanly."
    - text: 3:38 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: "yes"
    - paragraph: Delete tool
    - paragraph:
      - text: Direct deletion is blocked because the platform's own tracked reference to
      - code: fetch_leave_balance2
      - text: (in
      - code: state.tool_names
      - text: ) hasn't been cleared — even though the visible prompt text doesn't literally say "fetch_leave_balance2" anywhere. This is the same reference driving the earlier prompt-save rejections.
    - paragraph:
      - text: The fix is to update the prompt with an explicit, harmless mention of
      - code: fetch_leave_balance2
      - text: first (so the platform's reference-removal step has something to actually strip), then delete it right after. Let me do that in two clean steps.
    - paragraph: Update prompt
    - paragraph:
      - text: Same wall. The system auto-deregisters any tool not mentioned by name in the new prompt, and that auto-deregister always 409s for
      - code: fetch_leave_balance2
      - text: because something in the app's
      - code: tool_runner_parameters
      - text: /other layer still holds a reference — this is a genuine platform-side inconsistency that my available tools can't clear from here. Deletion also fails for the same underlying reason.
    - paragraph: "I don't want to keep re-submitting the same call and risk masking a real inconsistency in your agent's config. Here's where things actually stand, plainly:"
    - list:
      - listitem:
        - text: Your prompt content update (switching from
        - code: lookup_employee
        - text: to
        - code: fetch_leave_balance
        - text: ) is technically valid and passes validation every time, but the save is fully rejected each time because of this stuck tool reference — so the live prompt is still the old
        - code: lookup_employee
        - text: version right now.
      - listitem:
        - code: fetch_leave_balance2
        - text: can't be deleted or cleanly dropped from this app via the tools I have — every attempt hits the same conflict.
    - paragraph:
      - text: "Given that, the pragmatic path forward is to stop fighting the deregister step: keep"
      - code: fetch_leave_balance2
      - text: in the tool list (harmless, unused) and just make sure the prompt only
      - emphasis: calls
      - code: fetch_leave_balance
      - text: ". I'll retry once more, explicitly keeping it named passively in the text so nothing tries to remove it:"
    - paragraph: Get config
    - paragraph:
      - text: I've hit a hard blocker on my end that isn't going to resolve by retrying — the live app still has the old
      - code: lookup_employee
      - text: prompt and the tool-registry conflict on
      - code: fetch_leave_balance2
      - text: is stuck regardless of how the instructions are phrased.
    - paragraph: "Here's where things stand honestly:"
    - list:
      - listitem:
        - text: Your agent's live prompt still calls
        - code: lookup_employee
        - text: ", not"
        - code: fetch_leave_balance
        - text: . My attempts to switch it keep getting rejected by a stuck backend conflict on a leftover tool (
        - code: fetch_leave_balance2
        - text: ) that can't be cleanly removed or deleted right now.
      - listitem: This is a platform-side tool-sync inconsistency in this app, not something caused by your instructions or my wording choices.
    - paragraph: "Given that, I'd suggest one of two paths:"
    - list:
      - listitem:
        - text: I keep the current working prompt as-is (it already functions correctly with
        - code: lookup_employee
        - text: ") and we simply rename our thinking:"
        - code: lookup_employee
        - text: is your active leave-balance tool, and
        - code: fetch_leave_balance
        - text: /
        - code: fetch_leave_balance2
        - text: stay unused, retained only as dormant registry entries. No further mutation risk.
      - listitem:
        - text: You confirm you're fine leaving the two unused tools in place permanently (can't be deleted right now), and I retry the switch to
        - code: fetch_leave_balance
        - text: once more later — but I don't want to keep re-attempting the same failing call in this session since it's not converging.
    - paragraph:
      - text: Which would you prefer — stick with
      - code: lookup_employee
      - text: as the working tool now, or should I keep trying to force the switch to
      - code: fetch_leave_balance
      - text: "?"
    - text: 3:39 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: fetch_leave_balance
    - paragraph:
      - text: Got it — sticking with
      - code: fetch_leave_balance
      - text: as the active tool. Let me update the prompt to reference it directly, keeping everything else intact.
    - paragraph: Update prompt
    - paragraph:
      - text: Same conflict — the switch to
      - code: lookup_employee
      - text: →
      - code: fetch_leave_balance
      - text: triggers the system's auto-cleanup of the now-unmentioned tools (
      - code: lookup_employee
      - text: and
      - code: fetch_leave_balance2
      - text: ), and that cleanup step keeps failing on
      - code: fetch_leave_balance2
      - text: specifically, blocking the whole save.
    - paragraph:
      - text: I can route around this by keeping
      - code: fetch_leave_balance2
      - text: explicitly named in the prompt too, so it isn't a candidate for auto-removal. Let's have the agent try
      - code: fetch_leave_balance
      - text: first and fall back to
      - code: fetch_leave_balance2
      - text: only as a technical retry path — this keeps both tools referenced and avoids the deregister conflict entirely.
    - paragraph: Update prompt
    - paragraph:
      - text: This is a genuine platform-side inconsistency, not something my prompt phrasing can route around — the system is trying to deregister
      - code: fetch_leave_balance2
      - text: regardless of whether it's mentioned, and that deregister always 409s. I won't keep retrying the same failing call.
    - paragraph:
      - text: "Here's the honest status: your live prompt is unchanged — it's still on"
      - code: lookup_employee
      - text: ", exactly as before. Every attempt to switch it to"
      - code: fetch_leave_balance
      - text: fails at the last step due to a stuck tool-registry conflict on
      - code: fetch_leave_balance2
      - text: that I can't clear from here.
    - paragraph: "Two ways forward:"
    - list:
      - listitem:
        - text: Leave it as-is.
        - code: lookup_employee
        - text: is working correctly today and there's no functional gap — the agent looks up leave balances fine. If you're fine with the tool name difference being purely internal, we don't need to touch it.
      - listitem:
        - text: Escalate the stuck tool. This looks like a backend data inconsistency on
        - code: fetch_leave_balance2
        - text: "'s registration that needs a fix outside what my config tools can do — worth flagging to the Sarvam team so it gets cleared, after which the swap will go through cleanly."
    - paragraph:
      - text: Want me to leave the working
      - code: lookup_employee
      - text: setup in place for now?
    - text: 3:40 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Review this agent
    - paragraph: List suggestions
    - paragraph: List suggestions
    - paragraph: Agent authoring/references/quality rubric.md
    - paragraph:
      - text: The one previously-dismissed finding (about
      - code: lookup_employee
      - text: not being enabled) no longer applies —
      - code: lookup_employee
      - text: is enabled and correctly referenced now. Let me check the current draft against the rubric fresh.
    - paragraph: "Key things I see in the current draft:"
    - list:
      - listitem:
        - text: Prompt correctly uses
        - code: lookup_employee
        - text: — tool exists and is enabled. Good.
      - listitem:
        - code: send_payment_link_realtime
        - text: and
        - code: fetch_leave_balance
        - text: /
        - code: fetch_leave_balance2
        - text: are enabled but never referenced in the prompt (orphan tools) — flag as severity 1 (copilot review context).
      - listitem:
        - text: "Markdown-escaped underscores throughout:"
        - code: lookup\_employee
        - text: ","
        - code: end\_interaction
        - text: — this is a structural defect (escaped underscore breaks the tool/directive syntax read).
      - listitem: Check voicemail, nudges, goal — all present and fine from agent_state.
      - listitem: Check KB clause — attached KB but let me verify the retrieval clause requirement is met by checking config; not shown directly in state, but the prompt does reference "the attached HR leave policy knowledge base" only narratively, not with explicit topic-triggering instruction in the required format.
    - paragraph: Let me verify exact substrings before drafting suggestions.
    - paragraph: "Looking at the draft text carefully:"
    - list:
      - listitem:
        - code: "\"the lookup\\_employee tool\""
        - text: appears multiple times (Environment & Situation, Facts).
      - listitem:
        - code: "\"lookup\\_employee is called\""
        - text: — passive phrasing, should be imperative
        - code: call tool:lookup_employee
        - text: .
      - listitem:
        - code: "\"call end\\_interaction\""
        - text: "— escaped underscore, appears multiple times (5 occurrences: steps 5, 1, 1, 3 in Universal Handling... let me count for uniqueness)."
    - paragraph:
      - text: Since
      - code: call end\_interaction
      - text: appears more than once, I can't use it as a unique anchor. I need to find smaller unique-appearing spans or use the specific sentences containing it.
    - paragraph: Let me check exact occurrences to anchor properly.
    - paragraph:
      - text: Looking at the raw instructions text (from agent_state), occurrences of
      - code: end\_interaction
      - text: ":"
    - list:
      - listitem: "\"then call end_interaction.\" (step 5)"
      - listitem: "\"then call end_interaction.\" (Universal Handling 1, \"If the user agrees to a callback\")"
      - listitem: "\"then call end_interaction.\" (Universal Handling 3, hostile)"
    - paragraph: These three are identical substrings — not unique. I'll flag this as one structural issue citing the general pattern instead, using a slightly longer unique context sentence for each occurrence separately, or flag via a broader unique sentence.
    - paragraph: "Let me pick unique sentences that contain the escaped-underscore issue:"
    - list:
      - listitem: "\"Thanks are offered for calling, then call end_interaction.\" — unique (step 5 only)."
      - listitem: "\"If the user agrees to a callback: thanks are offered, then call end_interaction.\" — unique (Universal Handling 1)."
      - listitem: "\"Thanks are offered politely for their time, then call end_interaction.\" — unique (Universal Handling 3, hostility)."
    - paragraph: Good, each full sentence is unique even though the trailing fragment repeats. I'll anchor on these three separately, or better, batch as one finding type but need distinct sentence anchors per suggestion — I'll create one suggestion per unique occurrence, but that's redundant. I'll pick the most illustrative one and mention the pattern recurs.
    - paragraph: "Also check \"lookup_employee is called\" — full sentence: \"When the employee ID is provided, lookup_employee is called to fetch the leave balance.\" — unique, and it has two issues: escaped underscore AND passive phrasing."
    - paragraph: "Now the tool description references: \"using the lookup_employee tool\" appears in Environment & Situation and again in Facts — let me check exact text for uniqueness."
    - paragraph: "Environment & Situation: \"Leave balance lookups (via employee ID) are handled using the lookup_employee tool.\" Facts: \"Leave balance lookups require the employee's ID and are done via the lookup_employee tool.\""
    - paragraph: These are different sentences, each unique. Both have escaped underscore issue but that's a byproduct of markdown rendering — actually likely just markdown italics/underscore escaping artifact, not necessarily something to fix as syntax error but worth flagging since it could break tool-name matching at runtime.
    - paragraph: Let me finalize suggestions now.
    - paragraph: Create suggestions
    - paragraph: "Review complete — five findings recorded, all centered on one real issue: the leave-lookup step is currently a mess of conflicting tool syntax."
    - paragraph: "What's solid: the greeting flow, employee-ID collection, leave-policy KB fallback, off-topic redirect, busy/callback handling, and hostility handling are all present and well-branched. Voicemail, silence nudges, and the call goal are configured with agent-specific copy. Good coverage overall — this isn't a happy-path-only draft."
    - paragraph: "What needs fixing (flagged as suggestions):"
    - list:
      - listitem:
        - text: The critical lookup step (step 2 in Conversation Guidelines) currently reads
        - code: lookup_employee is called Call tool:fetch_leave_balance with <parameters>
        - text: — it names two different tools in one broken sentence with an unfilled placeholder. This is the top-priority fix; as written, it's genuinely ambiguous which tool actually runs.
      - listitem:
        - text: Every
        - code: call end_interaction
        - text: directive across the prompt has a markdown-escaped underscore (
        - code: end\_interaction
        - text: ), which risks the directive not resolving correctly at runtime — three occurrences flagged.
      - listitem:
        - text: The tool name
        - code: lookup_employee
        - text: itself is also escaped (
        - code: lookup\_employee
        - text: ) in the narrative sections — lower severity, but worth cleaning for consistency.
    - paragraph: Run these through Fix with Genie whenever you're ready to resolve them — the tool-syntax cleanup is the one that matters most for call reliability.
    - text: 3:46 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: give correct information from the knowledge base whenever user ask a question. answer should be given from the data
    - paragraph: Get kb status
    - paragraph: "The KB is attached but has no files indexed yet — that's why grounding might feel unreliable. Also, the prompt still has the broken tool-call step from before. Let me fix both: tighten the KB grounding instruction and clean up the tool reference in one pass."
    - paragraph: Configure kb
    - paragraph: Update prompt
    - paragraph:
      - text: Same stuck tool-deregistration conflict is still blocking every save on this app — it's not something my prompt wording triggers; it fires on any mutation now because
      - code: fetch_leave_balance2
      - text: is stuck in a bad registry state. Neither the prompt cleanup nor the knowledge-base clause went through.
    - paragraph: I don't want to keep re-hitting a call that's guaranteed to fail. This needs the stuck tool cleared on the platform side before any further edits to this agent will save — including the KB-grounding fix you just asked for.
    - paragraph: "In the meantime, here's what I'd land the moment it's unblocked:"
    - list:
      - listitem:
        - text: Leave-lookup step rewritten to call
        - code: lookup_employee
        - text: cleanly (no more mixed/garbled tool references).
      - listitem: A stronger instruction that policy answers must come strictly from retrieved knowledge-base content, with an explicit "say so, don't guess" fallback.
      - listitem: A proper KB retrieval clause naming leave types, eligibility, accrual, carry-forward, application process, and holidays so the agent reliably triggers KB lookup on those questions.
    - paragraph: "One more thing worth flagging: your knowledge base currently has no files uploaded — it's attached but empty, so even with perfect grounding instructions there's nothing for it to retrieve from yet. Want to upload your leave policy document now? That's independent of the stuck-tool issue and I can do it right away."
    - text: 4:02 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Fix this suggestion.
    - paragraph: The call-ending directive has a markdown-escaped underscore (end_interaction instead of end_interaction), which can break the directive at runtime. The same escaped form recurs at the other two end_interaction call-sites in Universal Handling.
    - paragraph: "Related text: \"Thanks are offered for calling, then call end_interaction.\""
    - paragraph: "Suggested fix: Replace end_interaction with end_interaction (no backslash) everywhere it appears in the prompt."
    - paragraph: Commit version
    - paragraph: Get config
    - paragraph:
      - text: The routed sentence appears exactly once in the current draft, so I'll fix all three occurrences of the escaped
      - code: end\_interaction
      - text: in one pass while I'm in there. The tool_names list in this snapshot only shows
      - code: fetch_leave_balance
      - text: and
      - code: send_payment_link_realtime
      - text: (the
      - code: lookup_employee
      - text: reference in Environment/Facts is stale text) — but this fix is scoped strictly to the underscore escaping, so I won't touch tool references here.
    - paragraph: Update prompt
    - paragraph:
      - text: The save is still blocked — this time on
      - code: fetch_leave_balance
      - text: itself hitting the same stuck-deregister conflict, unrelated to my edit content. The prompt on the live app has not changed.
    - paragraph: Report fix skipped
    - paragraph:
      - text: I couldn't get this one through — every save attempt on this agent is being blocked by a stuck tool-registry conflict on the leave-balance lookup tools, not by anything wrong with the fix itself. The
      - code: end_interaction
      - text: underscore cleanup is ready to apply the moment that clears, but right now the live prompt is unchanged.
    - paragraph: This is a platform-side issue that needs clearing outside of what my tools can do here. I'd recommend flagging it to the Sarvam team so the stuck tool records get released — after that, I can push this fix (and the other pending ones) through in a single pass.
    - text: 4:08 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Fix this suggestion.
    - paragraph: Tool name is written with a markdown-escaped underscore (lookup_employee), which risks not matching the actual registered tool name at runtime.
    - paragraph: "Related text: \"Leave balance lookups (via employee ID) are handled using the lookup_employee tool.\""
    - paragraph: "Suggested fix: Write the tool reference as call tool:lookup_employee (no backslash) consistently throughout the prompt."
    - paragraph: Commit version
    - paragraph: Get config
    - paragraph: The routed sentence ("Leave balance lookups (via employee ID) are handled using the lookup_employee tool.") does not appear in the current draft at all — the live prompt actually reads "Call tool:fetch_leave_balance" in that spot, not "lookup_employee". This suggestion is stale relative to the current draft.
    - paragraph: Report fix skipped
    - paragraph:
      - text: Skipped this one — the sentence it's pointing at doesn't actually exist in the current draft. Your live prompt currently references
      - code: fetch_leave_balance
      - text: ", not"
      - code: lookup_employee
      - text: ", in that spot, so the suggestion is stale."
    - paragraph:
      - text: "Worth noting: this is separate from the earlier stuck tool-registry issue, and it's also a symptom of the same underlying mismatch — your prompt still points at"
      - code: fetch_leave_balance
      - text: while the actual configured webhook tool is named
      - code: lookup_employee
      - text: ". That naming drift is the real fix needed here once the registry conflict clears: align the prompt's tool references to the tool that's actually wired up."
    - text: 4:09 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: Fix this suggestion.
    - paragraph: "This step mixes two different tool-call conventions in one sentence: passive markdown-escaped \"lookup_employee is called\" alongside a literal \"Call tool:fetch_leave_balance with \" placeholder that was never filled in. It is unclear which tool the agent is meant to call at this decision point, and fetch_leave_balance is not documented anywhere else in the prompt as the lookup tool."
    - paragraph: "Related text: \"When the employee ID is provided, lookup_employee is called Call tool:fetch_leave_balance with <parameters> .\""
    - paragraph: "Suggested fix: Pick one tool consistently (lookup_employee, matching the rest of Facts and Environment & Situation) and phrase the call imperatively as \"call tool:lookup_employee with the employee ID\", removing the stray fetch_leave_balance placeholder text."
    - paragraph: Commit version
    - paragraph: Get config
    - paragraph: The routed sentence doesn't match the current draft either — the live text at that step reads "Call tool:fetch_leave_balance with <parameters> is called Called", not the "lookup_employee is called" phrasing quoted in the suggestion. This is stale.
    - paragraph: Report fix skipped
    - paragraph: Skipped again — the exact wording this suggestion targets isn't in the current draft (it's stale text from an earlier version).
    - paragraph:
      - text: "That said, the underlying problem it's pointing at is real and worth fixing directly: step 2 of your Conversation Guidelines currently reads \"Call tool:fetch_leave_balance with <parameters> is called Called\" — that's garbled (mixed passive/imperative, an unfilled placeholder, doubled \"is called Called\"), and it references"
      - code: fetch_leave_balance
      - text: while your actual working webhook tool is named
      - code: lookup_employee
      - text: .
    - paragraph:
      - text: "Once the tool-registry conflict from earlier clears and I can save prompt edits again, I'd fix this whole step in one pass: rewrite it as a clean imperative call to the correct tool ("
      - code: lookup_employee
      - text: ) with the employee ID, and align every other reference to that tool name throughout Environment & Situation, Facts, and this guideline. Want me to queue that as the next fix once saves are unblocked?
    - text: 4:09 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: remove Lookup Employee tool
    - paragraph: List tools
    - paragraph:
      - text: "Confirming before I remove it:"
      - code: lookup_employee
      - text: is the webhook tool that looks up an employee's leave balance by ID (currently enabled but not yet referenced correctly in your prompt — that's the naming mismatch flagged earlier). Deleting it will leave the agent with no working lookup tool unless
      - code: fetch_leave_balance
      - text: (the other, differently-configured leave lookup tool) takes over that role in the prompt.
    - paragraph:
      - text: "Just to confirm: do you want me to permanently delete"
      - code: lookup_employee
      - text: ", and keep"
      - code: fetch_leave_balance
      - text: as the one true lookup tool (and fix the prompt to reference it correctly)? Or did you mean something else by "Lookup Employee tool"?
    - text: 4:27 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: delete it
    - paragraph: Delete tool
    - paragraph:
      - code: lookup_employee
      - text: is deleted.
      - code: fetch_leave_balance
      - text: remains as the active leave-lookup tool, but the prompt still needs a cleanup pass (garbled step 2 text, stray placeholders) to reference it cleanly — want me to fix that now?
    - text: 4:28 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: make sure kb and fetch_leave_balance should both work
    - paragraph: Agent authoring
    - paragraph: Get kb status
    - paragraph:
      - text: "The KB is attached but has zero files indexed, and the prompt's tool-call phrasing for the lookup is still garbled. I'll fix both: rewrite the prompt cleanly around"
      - code: fetch_leave_balance
      - text: and
      - code: query_knowledge_base
      - text: ", and flag the empty KB."
    - paragraph: Configure kb
    - paragraph: Update prompt
    - paragraph:
      - text: Both calls hit an unrelated backend conflict — it tried to deregister
      - code: fetch_leave_balance
      - text: as part of syncing tool state, and failed because the tool is actively referenced in the prompt (which is correct — we want to keep it referenced). Let me re-check the current state and retry.
    - paragraph: Get config
    - paragraph:
      - text: Both edits rolled back — state instructions are unchanged and KB clause wasn't applied. The failure is a backend registry glitch (stale
      - code: lookup_employee
      - text: reference in
      - code: tool_runner_parameters
      - text: still lingering from before deletion, conflicting on every save). Let me retry the prompt update alone first, since it's the higher priority.
    - paragraph: Update prompt
    - paragraph:
      - text: The build pipeline is trying to deregister
      - code: fetch_leave_balance
      - text: on every save because the stale
      - code: tool_runner_parameters.lookup_employee
      - text: entry left behind from the deleted tool is confusing the tool-sync step, and it's picking
      - code: fetch_leave_balance
      - text: as collateral. I need to reference
      - code: query_knowledge_base
      - text: explicitly as an enabled tool for this state, since it may be missing from
      - code: tool_names
      - text: .
    - paragraph: List tools
    - paragraph:
      - code: lookup_employee
      - text: is back — the earlier delete didn't stick on the backend, and this ghost tool is what's colliding on every save (the system tries to reconcile the tool registry and hits a conflict with
      - code: fetch_leave_balance
      - text: ). Let me delete it cleanly again before retrying the prompt/KB changes.
    - paragraph: Delete tool
    - paragraph: Update prompt
    - paragraph:
      - text: Same failure persists even without
      - code: lookup_employee
      - text: in the way — this is a build pipeline bug unrelated to my prompt content, tied to how
      - code: fetch_leave_balance
      - text: syncs. It's blocking every save attempt right now, including the earlier suggestion fix. This needs platform-side recovery beyond what I can resolve with the tools available.
    - paragraph: "Here's where things stand:"
    - list:
      - listitem:
        - code: lookup_employee
        - text: "tool: removed from your agent, as requested."
      - listitem:
        - code: fetch_leave_balance
        - text: "tool: still active and correctly wired for leave lookups, but I'm currently blocked from saving"
        - emphasis: any
        - text: prompt or KB change — the backend keeps rejecting the save with a conflict tied to
        - code: fetch_leave_balance
        - text: "'s registration, independent of what I'm editing."
      - listitem:
        - text: "Knowledge base: attached (id"
        - code: 01a0c814...
        - text: ), but it has zero files indexed right now — even once saves are unblocked, the KB won't answer policy questions until documents are uploaded.
    - paragraph: "Two things I need from you to get both working:"
    - list:
      - listitem: Do you want to upload the HR leave policy documents to the knowledge base? I can open the upload flow for that.
      - listitem:
        - text: The tool save conflict looks like a stuck backend state around
        - code: fetch_leave_balance
        - text: — if it doesn't clear on its own shortly, you may need to recreate that tool from scratch (delete and re-add with the same config) to reset it. Want me to do that now?
    - text: 4:30 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: kb is uploaded modify the instruction'
    - paragraph: Get kb status
    - paragraph: List tools
    - paragraph:
      - text: "Good news: the KB now has one file,"
      - code: HR_Leave_Policy_Knowledge_Base.pdf
      - text: ", status ready."
      - code: lookup_employee
      - text: regenerated again as a ghost — this seems to be an unstoppable auto-recreated stub that isn't actually referenced anywhere and isn't the cause of the earlier conflict (that was
      - code: fetch_leave_balance
      - text: itself, which is correctly in use). I'll leave
      - code: lookup_employee
      - text: alone this time and just push the prompt update, since the KB file being ready may have unblocked the pipeline.
    - paragraph: Update prompt
    - paragraph:
      - text: Still the same conflict, unrelated to my edit content. It's a persistent backend sync bug tied specifically to
      - code: fetch_leave_balance
      - text: "'s tool ID. Let me try nudging it via"
      - code: update_tool
      - text: (a no-op description refresh) to see if that resyncs its registration state before retrying the prompt save.
    - paragraph: Update tool
    - text: Summary Agent has been updated.
    - paragraph:
      - text: That resynced it —
      - code: fetch_leave_balance
      - text: re-registered cleanly and
      - code: send_payment_link_realtime
      - text: dropped from
      - code: tool_names
      - text: (it wasn't a real registered tool anyway). Now retrying the prompt update and the KB clause.
    - paragraph: Configure kb
    - paragraph: Update prompt
    - text: Summary Agent has been updated.
    - paragraph:
      - text: The KB clause saved fine, but there's no
      - code: query_knowledge_base
      - text: tool on this platform — retrieval is triggered by the clause itself, not by a named
      - code: "call tool:"
      - text: reference. I'll drop that phrasing and just describe KB use naturally, keeping
      - code: tool:fetch_leave_balance
      - text: for the real tool.
    - paragraph: Update prompt
    - paragraph:
      - text: The validator is choking on the trailing period after
      - code: fetch_leave_balance.
      - text: in my sentence ("calling tool:fetch_leave_balance with the employee ID." — the parser is grabbing the period into the tool name). Let me fix the phrasing to avoid punctuation immediately after the tool reference.
    - paragraph: Update prompt
    - text: Summary Agent has been updated.
    - paragraph: "That saved cleanly. Both pieces are now correctly wired:"
    - list:
      - listitem:
        - code: fetch_leave_balance
        - text: ": cleaned up and referenced clearly in the prompt for employee ID lookups."
      - listitem: "Knowledge base: your uploaded policy document is indexed and ready, and the agent's instructions now point to it for leave-policy questions, with a matching retrieval clause set so it's triggered for the right topics."
    - paragraph: Both are working together the way you wanted — lookups and policy Q&A are clearly separated in the flow, with no ambiguous overlap.
    - paragraph: "One thing worth doing next: since this is currently English-only, want me to add Hindi (or other regional languages) so it matches how employees actually call in? Also happy to set up a quick test scenario to validate both paths end-to-end if you want confidence before rollout."
    - text: Summary Agent has been updated. 4:44 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
    - paragraph: in greeting ask Please share your employee ID to check your leave balance or check leave policy. add this in greeting
    - paragraph: Update identity
    - text: Summary Agent has been updated.
    - paragraph: "Done — the greeting now offers both paths clearly:"
    - paragraph: "\"Namaste, this is Manan from Sarvam HR. Please share your employee ID to check your leave balance, or ask me a question about leave policy.\""
    - paragraph: Translations refreshed automatically for all your supported languages. Callers now hear upfront that they can either look up their balance or ask a policy question.
    - text: Summary Agent has been updated. 6:25 PM
    - button "Copy message"
    - button "Good response"
    - button "Bad response"
  - textbox "Message":
    - /placeholder: Ask Genie to change this agent…
  - button "Send" [disabled]
- alert: Indus by Sarvam
```

# Test source

```ts
  1   | import { Page, Locator, expect } from '@playwright/test';
  2   | 
  3   | /**
  4   |  * Drives the "Settings" tab of an agent's Build page: creating/attaching a
  5   |  * knowledge base to that specific agent, and detaching it again. Actual KB
  6   |  * creation (folder name, description, file upload) reuses KBPage's dialog
  7   |  * methods, since the "Create knowledge base" dialog reached from here is the
  8   |  * same component as the one on the standalone Knowledge base list page.
  9   |  */
  10  | export class AgentKBPage {
  11  |   private readonly settingsButton: Locator;
  12  |   private readonly closeButton: Locator;
  13  |   private readonly selectKbButton: Locator;
  14  |   private readonly kbSearchInput: Locator;
  15  |   private readonly createDialog: Locator;
  16  | 
  17  |   constructor(private page: Page) {
  18  |     this.settingsButton = page.getByRole('button', { name: 'Settings' });
  19  |     // exact: true -- an unrelated, persistently-visible "Close panel" button
  20  |     // elsewhere on this page otherwise satisfies a substring match on "Close"
  21  |     // too, which would make toBeHidden() below wait on the wrong element.
  22  |     this.closeButton = page.getByRole('button', { name: 'Close', exact: true });
  23  |     this.selectKbButton = page.getByRole('button', { name: 'Select knowledge bases' });
  24  |     this.kbSearchInput = page.getByRole('textbox', { name: 'Search...' });
  25  |     this.createDialog = page.getByRole('dialog');
  26  |   }
  27  | 
  28  |   // =========================================================
  29  |   // NAVIGATE
  30  |   // =========================================================
  31  | 
  32  |   async openSettings() {
  33  |     await expect(this.settingsButton).toBeVisible({ timeout: 20_000 });
  34  |     await this.settingsButton.click();
  35  |   }
  36  | 
  37  |   // =========================================================
  38  |   // CREATE (dialog itself is driven via KBPage)
  39  |   // =========================================================
  40  | 
  41  |   /**
  42  |    * This agent's Build page renders a Genie chat history panel alongside the
  43  |    * create-KB dialog, and past sessions can happen to mention the same
  44  |    * filename in their transcript -- a page-wide getByText() (as used by
  45  |    * KBPage.verifyFileListed) then matches both and fails strict mode.
  46  |    * Scoping to the dialog itself avoids that collision.
  47  |    */
  48  |   async verifyFileListedInCreateDialog(fileName: string) {
  49  |     await expect(this.createDialog.getByText(fileName)).toBeVisible({
  50  |       timeout: 30_000
  51  |     });
  52  |   }
  53  | 
  54  |   /**
  55  |    * A newly created KB auto-attaches to the agent and leaves this
  56  |    * confirmation dialog open -- later steps (search box, chip locators)
  57  |    * resolve elsewhere on the page and can be covered by an unrelated
  58  |    * lingering dialog if this isn't dismissed first.
  59  |    */
  60  |   async closeCreatedKbDialog() {
  61  |     await expect(this.closeButton).toBeVisible({ timeout: 15_000 });
  62  |     await this.closeButton.click();
  63  |     await expect(this.closeButton).toBeHidden({ timeout: 10_000 });
  64  |   }
  65  | 
  66  |   // =========================================================
  67  |   // ATTACH / DETACH
  68  |   // =========================================================
  69  | 
  70  |   /**
  71  |    * An attached KB renders as a chip whose accessible name concatenates its
  72  |    * folder name with its inline remove icon's own label ("<folderName>
  73  |    * Remove"), so scoping by that full string is what uniquely identifies the
  74  |    * chip -- the icon inside it is what's actually clickable to detach it.
  75  |    */
  76  |   private kbChip(folderName: string): Locator {
  77  |     return this.page.getByRole('button', { name: `${folderName} Remove` });
  78  |   }
  79  | 
  80  |   async verifyKnowledgeBaseAttached(folderName: string) {
  81  |     await expect(this.kbChip(folderName)).toBeVisible({ timeout: 15_000 });
  82  |   }
  83  | 
  84  |   async removeKnowledgeBase(folderName: string) {
  85  |     const chip = this.kbChip(folderName);
> 86  |     await expect(chip).toBeVisible({ timeout: 15_000 });
      |                        ^ Error: expect(locator).toBeVisible() failed
  87  |     await chip.getByLabel('Remove').click();
  88  |     await expect(chip).toBeHidden({ timeout: 15_000 });
  89  |   }
  90  | 
  91  |   async openKnowledgeBaseSelector() {
  92  |     await this.selectKbButton.click();
  93  |     await expect(this.kbSearchInput).toBeVisible({ timeout: 10_000 });
  94  |   }
  95  | 
  96  |   async searchKnowledgeBaseOption(name: string) {
  97  |     await this.kbSearchInput.fill(name);
  98  |   }
  99  | 
  100 |   /** The option's accessible name is "<folderName> <description>". */
  101 |   async selectKnowledgeBaseOption(folderName: string, description: string) {
  102 |     const option = this.page.getByRole('option', { name: `${folderName} ${description}` });
  103 |     await expect(option).toBeVisible({ timeout: 15_000 });
  104 |     await option.click();
  105 |   }
  106 | 
  107 |   /** Full manual-attach flow: open the selector, search, and pick the matching KB. */
  108 |   async attachKnowledgeBase(folderName: string, description: string) {
  109 |     await this.openKnowledgeBaseSelector();
  110 |     await this.searchKnowledgeBaseOption(folderName);
  111 |     await this.selectKnowledgeBaseOption(folderName, description);
  112 |     await this.verifyKnowledgeBaseAttached(folderName);
  113 |   }
  114 | }
  115 | 
```